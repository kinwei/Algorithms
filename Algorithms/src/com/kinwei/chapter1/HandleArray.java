package com.kinwei.chapter1;public class HandleArray {	/**	 * @param args	 */		public int findMaxItem(int[] arr){		int max = arr[0];		for (int i = 0; i < arr.length; i++) {			if(arr[i] > max){				max = arr[i];			}		}		return max;	}		public double average(double[] arr){		double sum = 0.0;		for (int i = 0; i < arr.length; i++) {			sum += arr[i];		}		return sum/arr.length;	}		public double[] copyArray(double[] arr){		double[] b = new double[arr.length];		for (int i = 0; i < b.length; i++) {			b[i] = arr[i];		}		return b;	}				public static void main(String[] args) {		// TODO Auto-generated method stub		HandleArray h = new HandleArray();		double a[] = {1,4,2,5,56,32,1};		//System.out.println(h.findMaxItem(a));		System.out.println(h.average(a));	}}